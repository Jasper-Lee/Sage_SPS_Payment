({
    /* handleInvoiceDropdown : function(component, event, helper)
    {
        helper.invoiceDropdown(component, event, helper);
    }, */
    handleEdit : function(component, event, helper)
    {
        helper.edit(component, event, helper);
    },
    handleSave : function(component, event, helper)
    {
        helper.save(component, event, helper);
    },
    handleCancel : function(component, event, helper)
    {
        helper.cancel(component, event, helper);
    },
    handleFormLoad : function(component, event, helper)
    {
        helper.formLoad(component, event, helper);
    },
    handleContinuePayment : function(component, event, helper)
    {
        helper.continuePayment(component, event, helper);
    }
})