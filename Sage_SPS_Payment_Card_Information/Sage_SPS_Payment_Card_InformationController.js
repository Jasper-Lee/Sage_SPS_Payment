({
	handleBillingAddressCheckbox : function(component, event, helper)
    {
        helper.billingAddressCheckbox(component, event, helper);
    },
    handleEdit : function(component, event, helper)
    {
        helper.edit(component, event, helper);
    },
    handleSave : function(component, event, helper)
    {
        helper.save(component, event, helper);
    },
    handlePrimary : function(component, event, helper)
    {
        helper.primary(component, event, helper);
    }
})